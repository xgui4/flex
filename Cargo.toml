[package]
name = "flex"
version = "0.0.1"
edition = "2024"
build = "build.rs"

[build-dependencies]
windows_exe_info = "0.5.2"

[dependencies]
windows_exe_info = {version = "0.5.2", features = ["manifest"]}